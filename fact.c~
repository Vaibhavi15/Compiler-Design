#include<stdio.h>
int main()
{

// test

int n, fact=1;
int 9d;
char* a = "amit;
scanf("%d", &n);
do
{
	fact=fact*n;
	n--;
}while(n>0);

if(fact!=-1)
	printf("Factorial is %d", fact);

	// test1
	/*This
	is a
	multiline comment*/

return 0;
}
